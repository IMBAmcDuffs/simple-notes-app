## Infrastructure Context (MCP Tools)

The following data was retrieved from infrastructure inspection tools. Use it to understand the current state of the project's environment.

### compose_topology
*Parse docker-compose.yml and return service graph with networks and volumes*

```
{"error": "Compose file not found: /app/docker-compose.yml"}
```

### core_version
*Get WordPress core version and check for available updates*

```
{
  "current_version": {
    "error": "Error: Parameter errors:\n unknown --format parameter"
  },
  "available_updates": {
    "error": "Error: This does not seem to be a WordPress installation.\nThe used path is: /var/www/html/\nPass --path=`path/to/wordpress` or run `wp core download`."
  }
}
```

### plugin_list
*List installed WordPress plugins with status and version*

```
{"error": "Error: This does not seem to be a WordPress installation.\nThe used path is: /var/www/html/\nPass --path=`path/to/wordpress` or run `wp core download`."}
```

### site_health
*Run WordPress site health checks and return results*

```
{
  "db_check": {
    "error": "Error: This does not seem to be a WordPress installation.\nThe used path is: /var/www/html/\nPass --path=`path/to/wordpress` or run `wp core download`."
  },
  "cache_type": {
    "error": "Error: This does not seem to be a WordPress installation.\nThe used path is: /var/www/html/\nPass --path=`path/to/wordpress` or run `wp core download`."
  },
  "cron_events": {
    "error": "Error: This does not seem to be a WordPress installation.\nThe used path is: /var/www/html/\nPass --path=`path/to/wordpress` or run `wp core download`."
  }
}
```
